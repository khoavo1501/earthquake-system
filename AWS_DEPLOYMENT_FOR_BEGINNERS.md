# 🚀 Hướng Dẫn Deploy Lên AWS Cho Người Mới Bắt Đầu

## 📋 Mục Lục
1. [Giới Thiệu AWS](#1-giới-thiệu-aws)
2. [Tạo Tài Khoản AWS](#2-tạo-tài-khoản-aws)
3. [Cài Đặt Công Cụ Cần Thiết](#3-cài-đặt-công-cụ-cần-thiết)
4. [Tạo EC2 Instance (Máy Chủ Ảo)](#4-tạo-ec2-instance-máy-chủ-ảo)
5. [Kết Nối Vào Server](#5-kết-nối-vào-server)
6. [Cài Đặt Docker Trên Server](#6-cài-đặt-docker-trên-server)
7. [Upload Code Lên Server](#7-upload-code-lên-server)
8. [Chạy Ứng Dụng](#8-chạy-ứng-dụng)
9. [Cấu Hình Domain (Tùy Chọn)](#9-cấu-hình-domain-tùy-chọn)
10. [Các Lỗi Thường Gặp](#10-các-lỗi-thường-gặp)

---

## 1. Giới Thiệu AWS

### AWS là gì?
**Amazon Web Services (AWS)** là nền tảng điện toán đám mây lớn nhất thế giới, cung cấp các dịch vụ như:
- **EC2**: Máy chủ ảo (Virtual Server)
- **RDS**: Database được quản lý
- **S3**: Lưu trữ file
- **Route 53**: Quản lý domain

### Tại sao chọn AWS?
- ✅ Free Tier 12 tháng cho người mới
- ✅ Tài liệu phong phú
- ✅ Cộng đồng hỗ trợ lớn
- ✅ Dễ scale khi cần

### Chi phí ước tính
| Dịch vụ | Free Tier | Sau Free Tier |
|---------|-----------|---------------|
| EC2 t2.micro | 750 giờ/tháng miễn phí | ~$8.5/tháng |
| EC2 t2.medium | Không miễn phí | ~$33/tháng |
| RDS (nếu dùng) | 750 giờ/tháng miễn phí | ~$15/tháng |

---

## 2. Tạo Tài Khoản AWS

### Bước 2.1: Truy cập trang đăng ký

1. Mở trình duyệt, vào: **https://aws.amazon.com**
2. Click nút **"Create an AWS Account"** (góc phải trên)

```
┌─────────────────────────────────────────────────────────────┐
│  🌐 aws.amazon.com                                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   [aws logo]                    [Sign In] [Create Account]  │
│                                              ↑              │
│                                         Click vào đây       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Bước 2.2: Điền thông tin tài khoản

```
┌─────────────────────────────────────────────────────────────┐
│                    Create an AWS account                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Root user email address:                                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ your-email@gmail.com                                │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  AWS account name:                                          │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ MyFirstAWSAccount                                   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│           [Verify email address]                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**Lưu ý quan trọng:**
- ⚠️ Sử dụng email bạn có quyền truy cập
- ⚠️ Ghi nhớ email và mật khẩu này
- ⚠️ Email này sẽ là **Root Account** (tài khoản gốc)

### Bước 2.3: Xác minh email

1. AWS sẽ gửi mã xác minh đến email của bạn
2. Nhập mã 6 chữ số vào form

```
┌─────────────────────────────────────────────────────────────┐
│                    Verify email address                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  We sent a verification code to your-email@gmail.com        │
│                                                             │
│  Verification code:                                         │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 1 2 3 4 5 6                                         │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│           [Verify]                                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Bước 2.4: Tạo mật khẩu Root

```
┌─────────────────────────────────────────────────────────────┐
│                    Create your password                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Root user password:                                        │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ••••••••••••••••                                    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  Confirm password:                                          │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ••••••••••••••••                                    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  Password requirements:                                     │
│  ✅ Minimum 8 characters                                    │
│  ✅ Include uppercase letters                               │
│  ✅ Include lowercase letters                               │
│  ✅ Include numbers                                         │
│  ✅ Include special characters (!@#$%^&*)                   │
│                                                             │
│           [Continue]                                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Bước 2.5: Thông tin liên hệ

Chọn **"Personal"** (cá nhân) cho mục đích học tập:

```
┌─────────────────────────────────────────────────────────────┐
│                    Contact Information                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Account type:                                              │
│  ○ Business    ● Personal  ← Chọn Personal                  │
│                                                             │
│  Full name:        [Nguyen Van A                    ]       │
│  Phone number:     [+84 123456789                   ]       │
│  Country/Region:   [Vietnam                    ▼    ]       │
│  Address:          [123 Nguyen Van Linh             ]       │
│  City:             [Ho Chi Minh City                ]       │
│  State/Province:   [Ho Chi Minh                     ]       │
│  Postal code:      [700000                          ]       │
│                                                             │
│  ☑ I agree to the AWS Customer Agreement                    │
│                                                             │
│           [Continue]                                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Bước 2.6: Thêm thông tin thanh toán

⚠️ **QUAN TRỌNG**: AWS yêu cầu thẻ tín dụng/ghi nợ quốc tế (Visa/Mastercard)

```
┌─────────────────────────────────────────────────────────────┐
│                    Billing Information                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Credit or Debit card number:                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 4111 1111 1111 1111                                 │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  Expiration date:    CVV/CVC:                               │
│  ┌─────────────┐     ┌─────────────┐                       │
│  │ 12/2026     │     │ 123         │                       │
│  └─────────────┘     └─────────────┘                       │
│                                                             │
│  Billing address: ● Same as contact address                 │
│                                                             │
│           [Verify and Continue]                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**Lưu ý:**
- 💳 AWS sẽ trừ **$1 USD** để xác minh thẻ (sẽ hoàn lại sau 3-5 ngày)
- 💡 Nếu không có thẻ quốc tế, có thể dùng thẻ ảo từ ví điện tử (Momo, ZaloPay...)

### Bước 2.7: Xác minh danh tính

```
┌─────────────────────────────────────────────────────────────┐
│                    Confirm your identity                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  How should we send you the verification code?              │
│                                                             │
│  ● Text message (SMS)                                       │
│  ○ Voice call                                               │
│                                                             │
│  Country/Region code:  Phone number:                        │
│  ┌─────────────────┐   ┌─────────────────────────────┐     │
│  │ Vietnam (+84) ▼ │   │ 123456789                   │     │
│  └─────────────────┘   └─────────────────────────────┘     │
│                                                             │
│  Security check: [CAPTCHA]                                  │
│                                                             │
│           [Send SMS]                                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Bước 2.8: Chọn Support Plan

Chọn **"Basic support - Free"**:

```
┌─────────────────────────────────────────────────────────────┐
│                    Select a Support Plan                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  ● Basic support - Free          ← CHỌN CÁI NÀY     │   │
│  │    - Access to documentation                        │   │
│  │    - Support forums                                 │   │
│  │    - Service health dashboard                       │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  ○ Developer - $29/month                            │   │
│  │    - Business hours email support                   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  ○ Business - $100/month                            │   │
│  │    - 24/7 support                                   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│           [Complete sign up]                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### ✅ Hoàn tất đăng ký!

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│                    🎉 Congratulations!                       │
│                                                             │
│     Your AWS account is ready to use                        │
│                                                             │
│           [Go to the AWS Management Console]                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. Cài Đặt Công Cụ Cần Thiết

### 3.1 Cài đặt Git Bash (Windows)

1. Tải Git từ: **https://git-scm.com/downloads**
2. Chạy file cài đặt, chọn các tùy chọn mặc định
3. Sau khi cài xong, mở **Git Bash** từ Start Menu

### 3.2 Cài đặt AWS CLI

**Trên Windows:**
1. Tải AWS CLI: **https://awscli.amazonaws.com/AWSCLIV2.msi**
2. Chạy file .msi và làm theo hướng dẫn

**Kiểm tra cài đặt:**
```bash
aws --version
# Output: aws-cli/2.x.x Python/3.x.x Windows/10 exe/AMD64
```

### 3.3 Cấu hình AWS CLI

#### Bước 1: Tạo Access Key

1. Đăng nhập vào AWS Console: **https://console.aws.amazon.com**
2. Click vào tên tài khoản (góc phải trên) → **Security credentials**

```
┌─────────────────────────────────────────────────────────────┐
│  AWS Console                              [Account Name ▼]  │
├─────────────────────────────────────────────────────────────┤
│                                           │ Account         │
│                                           │ Organization    │
│                                           │ Security cred.. │ ← Click
│                                           │ Settings        │
│                                           │ Sign out        │
└─────────────────────────────────────────────────────────────┘
```

3. Kéo xuống phần **"Access keys"** → Click **"Create access key"**

```
┌─────────────────────────────────────────────────────────────┐
│                    Access keys                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Access keys are long-term credentials for an IAM user...   │
│                                                             │
│           [Create access key]  ← Click vào đây              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

4. Chọn **"Command Line Interface (CLI)"** → Tick xác nhận → **Create**

```
┌─────────────────────────────────────────────────────────────┐
│                    Access key best practices                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Select your use case:                                      │
│  ● Command Line Interface (CLI)  ← Chọn cái này             │
│  ○ Local code                                               │
│  ○ Application running on AWS                               │
│  ○ Third-party service                                      │
│                                                             │
│  ☑ I understand the above recommendation...                 │
│                                                             │
│           [Next]                                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

5. **LƯU LẠI** Access Key ID và Secret Access Key

```
┌─────────────────────────────────────────────────────────────┐
│                    Retrieve access keys                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ⚠️ This is the only time you can view or download the      │
│  secret access key. Save it securely.                       │
│                                                             │
│  Access key:                                                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ AKIAIOSFODNN7EXAMPLE                      [Copy 📋] │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  Secret access key:                                         │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY  [Copy 📋] │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│           [Download .csv file]    [Done]                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

⚠️ **QUAN TRỌNG**: Lưu ngay 2 giá trị này! Bạn sẽ KHÔNG thể xem lại Secret Access Key!

#### Bước 2: Cấu hình AWS CLI

Mở Git Bash hoặc Terminal, chạy:

```bash
aws configure
```

Nhập các thông tin:

```
AWS Access Key ID [None]: AKIAIOSFODNN7EXAMPLE
AWS Secret Access Key [None]: wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
Default region name [None]: ap-southeast-1
Default output format [None]: json
```

**Giải thích:**
- `ap-southeast-1` = Singapore (gần Việt Nam, latency thấp)
- Các region khác: `us-east-1` (Virginia), `ap-northeast-1` (Tokyo)

**Kiểm tra cấu hình:**
```bash
aws sts get-caller-identity
```

Output thành công:
```json
{
    "UserId": "AIDAIOSFODNN7EXAMPLE",
    "Account": "123456789012",
    "Arn": "arn:aws:iam::123456789012:root"
}
```

---

## 4. Tạo EC2 Instance (Máy Chủ Ảo)

### 4.1 Vào EC2 Dashboard

1. Đăng nhập AWS Console
2. Tìm kiếm **"EC2"** trong thanh search → Click vào **EC2**

```
┌─────────────────────────────────────────────────────────────┐
│  🔍 Search: EC2                                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Services                                                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  🖥️ EC2 - Virtual Servers in the Cloud              │   │ ← Click
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 Launch Instance

1. Click **"Launch Instance"** (nút màu cam)

```
┌─────────────────────────────────────────────────────────────┐
│  EC2 Dashboard                                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Resources                                                  │
│  ├── Instances (running): 0                                 │
│  ├── Volumes: 0                                             │
│  └── Security Groups: 1                                     │
│                                                             │
│  Launch instance                                            │
│  ┌───────────────────┐                                     │
│  │  Launch instance  │  ← Click vào đây                     │
│  └───────────────────┘                                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 Cấu hình Instance

#### Bước 1: Đặt tên

```
┌─────────────────────────────────────────────────────────────┐
│  Name and tags                                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Name:                                                      │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ earthquake-system-server                            │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### Bước 2: Chọn AMI (Hệ điều hành)

Chọn **Ubuntu Server 22.04 LTS (Free tier eligible)**:

```
┌─────────────────────────────────────────────────────────────┐
│  Application and OS Images (Amazon Machine Image)           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Quick Start:                                               │
│  [Amazon Linux] [macOS] [Ubuntu] [Windows] [Red Hat]        │
│                           ↑                                 │
│                      Click Ubuntu                           │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  🟢 Ubuntu Server 22.04 LTS (HVM), SSD Volume Type  │   │
│  │     Free tier eligible                              │   │
│  │     64-bit (x86)                                    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### Bước 3: Chọn Instance Type

Chọn **t2.medium** (khuyến nghị) hoặc **t2.micro** (Free tier):

```
┌─────────────────────────────────────────────────────────────┐
│  Instance type                                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Instance type:                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  t2.medium  ▼                                       │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌──────────────┬────────┬────────┬─────────────────┐     │
│  │ Type         │ vCPUs  │ Memory │ Free Tier       │     │
│  ├──────────────┼────────┼────────┼─────────────────┤     │
│  │ t2.micro     │ 1      │ 1 GB   │ ✅ Yes          │     │
│  │ t2.small     │ 1      │ 2 GB   │ ❌ No           │     │
│  │ t2.medium ⭐ │ 2      │ 4 GB   │ ❌ No (~$33/mo) │     │
│  └──────────────┴────────┴────────┴─────────────────┘     │
│                                                             │
│  💡 Khuyến nghị: t2.medium cho production                   │
│     t2.micro đủ để test nhưng có thể chậm                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### Bước 4: Tạo Key Pair (Rất quan trọng!)

Key Pair dùng để SSH vào server. **BẮT BUỘC phải tạo!**

1. Click **"Create new key pair"**

```
┌─────────────────────────────────────────────────────────────┐
│  Key pair (login)                                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Key pair name:                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Proceed without a key pair (Not recommended)   ▼   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  [Create new key pair]  ← Click vào đây                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

2. Điền thông tin Key Pair:

```
┌─────────────────────────────────────────────────────────────┐
│  Create key pair                                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Key pair name:                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ earthquake-key                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  Key pair type:                                             │
│  ● RSA                                                      │
│  ○ ED25519                                                  │
│                                                             │
│  Private key file format:                                   │
│  ● .pem (For OpenSSH, Linux, macOS, Git Bash)  ← Chọn này  │
│  ○ .ppk (For PuTTY on Windows)                              │
│                                                             │
│           [Create key pair]                                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

3. **File `earthquake-key.pem` sẽ tự động tải xuống**

⚠️ **CỰC KỲ QUAN TRỌNG:**
- Lưu file này ở nơi an toàn!
- **KHÔNG chia sẻ** file này với ai!
- Nếu mất file này, bạn **KHÔNG THỂ** SSH vào server!

4. Di chuyển file key đến thư mục an toàn:

```bash
# Tạo thư mục .ssh nếu chưa có
mkdir -p ~/.ssh

# Di chuyển key file
mv ~/Downloads/earthquake-key.pem ~/.ssh/

# Đặt quyền cho file (BẮT BUỘC!)
chmod 400 ~/.ssh/earthquake-key.pem
```

#### Bước 5: Cấu hình Network (Security Group)

Mở rộng phần **"Network settings"** → Click **"Edit"**:

```
┌─────────────────────────────────────────────────────────────┐
│  Network settings                                    [Edit] │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  VPC: vpc-xxx (default)                                     │
│  Subnet: No preference                                      │
│  Auto-assign public IP: Enable  ← Quan trọng!               │
│                                                             │
│  Firewall (security groups):                                │
│  ● Create security group                                    │
│  ○ Select existing security group                           │
│                                                             │
│  Security group name: earthquake-sg                         │
│                                                             │
│  Inbound Security Group Rules:                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │ Type        │ Protocol │ Port │ Source            │    │
│  ├─────────────┼──────────┼──────┼───────────────────┤    │
│  │ SSH         │ TCP      │ 22   │ 0.0.0.0/0         │    │
│  │ HTTP        │ TCP      │ 80   │ 0.0.0.0/0         │    │  ← Add
│  │ HTTPS       │ TCP      │ 443  │ 0.0.0.0/0         │    │  ← Add
│  │ Custom TCP  │ TCP      │ 3000 │ 0.0.0.0/0         │    │  ← Add
│  │ Custom TCP  │ TCP      │ 8000 │ 0.0.0.0/0         │    │  ← Add
│  │ Custom TCP  │ TCP      │ 8001 │ 0.0.0.0/0         │    │  ← Add
│  │ Custom TCP  │ TCP      │ 8002 │ 0.0.0.0/0         │    │  ← Add
│  │ Custom TCP  │ TCP      │ 8003 │ 0.0.0.0/0         │    │  ← Add
│  │ Custom TCP  │ TCP      │ 8004 │ 0.0.0.0/0         │    │  ← Add
│  └─────────────┴──────────┴──────┴───────────────────┘    │
│                                                             │
│  [Add security group rule]  ← Click để thêm rule           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**Giải thích các port:**
| Port | Mục đích |
|------|----------|
| 22 | SSH (kết nối server) |
| 80 | HTTP |
| 443 | HTTPS |
| 3000 | Frontend React |
| 8000 | Database API |
| 8001 | Data Ingestion |
| 8002 | Data Analysis |
| 8003 | Clustering |
| 8004 | Prediction |

#### Bước 6: Cấu hình Storage

```
┌─────────────────────────────────────────────────────────────┐
│  Configure storage                                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1x  [30]  GiB    gp3    Root volume                       │
│                   ↑                                         │
│             Thay từ 8 thành 30 GB                           │
│                                                             │
│  ℹ️ Free tier eligible customers can get up to 30 GB        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

💡 **Tip**: 30 GB nằm trong Free Tier, đủ dùng cho project này.

#### Bước 7: Review và Launch

1. Kiểm tra lại tất cả cấu hình
2. Click **"Launch instance"**

```
┌─────────────────────────────────────────────────────────────┐
│  Summary                                                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Number of instances:     1                                 │
│  Software Image (AMI):    Ubuntu Server 22.04 LTS           │
│  Instance type:           t2.medium                         │
│  Key pair:                earthquake-key                    │
│  Network:                 vpc-xxx (default)                 │
│  Security group:          earthquake-sg                     │
│  Storage:                 30 GiB gp3                        │
│                                                             │
│           [Launch instance]  ← Click!                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4.4 Lấy Public IP

1. Sau khi launch, click **"View all instances"**
2. Chờ Instance State chuyển thành **"Running"** (màu xanh)
3. Copy **Public IPv4 address**

```
┌─────────────────────────────────────────────────────────────┐
│  Instances                                                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Name                    │ Instance State │ Public IPv4     │
│  ────────────────────────┼────────────────┼────────────────│
│  earthquake-system-server│ 🟢 Running     │ 54.169.123.45  │
│                          │                │      ↑          │
│                          │                │  Copy IP này!   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 5. Kết Nối Vào Server

### 5.1 Sử dụng Git Bash (Windows)

Mở **Git Bash** và chạy:

```bash
# Kết nối SSH
ssh -i ~/.ssh/earthquake-key.pem ubuntu@54.169.123.45
```

**Thay `54.169.123.45` bằng Public IP của bạn!**

### 5.2 Xử lý lỗi thường gặp

#### Lỗi: "WARNING: UNPROTECTED PRIVATE KEY FILE!"

```
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@         WARNING: UNPROTECTED PRIVATE KEY FILE!          @
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
```

**Cách sửa:**
```bash
chmod 400 ~/.ssh/earthquake-key.pem
```

#### Lỗi: "Permission denied (publickey)"

**Nguyên nhân:** Sai tên user hoặc sai key file

**Cách sửa:**
```bash
# Ubuntu AMI dùng user "ubuntu"
ssh -i ~/.ssh/earthquake-key.pem ubuntu@IP_ADDRESS

# Amazon Linux dùng user "ec2-user"
ssh -i ~/.ssh/earthquake-key.pem ec2-user@IP_ADDRESS
```

### 5.3 Kết nối thành công

Khi thấy prompt như này là đã kết nối thành công:

```
Welcome to Ubuntu 22.04.3 LTS (GNU/Linux 5.15.0-1051-aws x86_64)

 * Documentation:  https://help.ubuntu.com

ubuntu@ip-172-31-xx-xx:~$ _
```

---

## 6. Cài Đặt Docker Trên Server

### 6.1 Cập nhật hệ thống

```bash
# Cập nhật package list
sudo apt update

# Upgrade các package hiện có
sudo apt upgrade -y
```

### 6.2 Cài đặt Docker

```bash
# Cài đặt các package cần thiết
sudo apt install -y apt-transport-https ca-certificates curl software-properties-common

# Thêm Docker GPG key
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

# Thêm Docker repository
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Cập nhật và cài Docker
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io
```

### 6.3 Cài đặt Docker Compose

```bash
# Tải Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose

# Cấp quyền thực thi
sudo chmod +x /usr/local/bin/docker-compose

# Kiểm tra version
docker-compose --version
```

### 6.4 Cấu hình Docker cho user ubuntu

```bash
# Thêm user ubuntu vào group docker
sudo usermod -aG docker ubuntu

# Áp dụng thay đổi (QUAN TRỌNG: phải logout và login lại!)
exit
```

Sau đó SSH lại vào server:
```bash
ssh -i ~/.ssh/earthquake-key.pem ubuntu@54.169.123.45
```

Kiểm tra Docker chạy được không cần sudo:
```bash
docker ps
# Nếu không báo lỗi permission là OK
```

---

## 7. Upload Code Lên Server

### Cách 1: Sử dụng Git (Khuyến nghị)z`

#### Bước 1: Push code lên GitHub

Trên máy local (Windows), trong thư mục project:

```bash
# Khởi tạo git repo (nếu chưa có)
cd d:/WorkSpace/KPDL/DO-AN-CUOI-KY/earthquake-system
git init

# Tạo .gitignore
cat > .gitignore << 'EOF'
__pycache__/
*.pyc
.env
node_modules/
*.log
.DS_Store
EOF

# Add và commit
git add .
git commit -m "Initial commit"

# Thêm remote (thay YOUR_USERNAME bằng username GitHub của bạn)
git remote add origin https://github.com/YOUR_USERNAME/earthquake-system.git

# Push code
git push -u origin main
```

#### Bước 2: Clone code trên server

SSH vào server và chạy:

```bash
# Clone repository
git clone https://github.com/YOUR_USERNAME/earthquake-system.git

# Vào thư mục project
cd earthquake-system
```

### Cách 2: Sử dụng SCP (Copy trực tiếp)

Trên máy local (Windows), mở Git Bash:

```bash
# Cách 2.1: Dùng đường dẫn Windows (khuyến nghị)
scp -i ~/.ssh/earthquake-key.pem -r "D:/WorkSpace/KPDL/DO-AN-CUOI-KY/earthquake-system" ubuntu@54.169.123.45:~/

# Cách 2.2: CD vào thư mục cha rồi copy
cd "/d/WorkSpace/KPDL/DO-AN-CUOI-KY"
scp -i ~/.ssh/earthquake-key.pem -r earthquake-system ubuntu@54.169.123.45:~/

# Cách 2.3: Copy từ thư mục hiện tại
cd "/d/WorkSpace/KPDL/DO-AN-CUOI-KY/earthquake-system"
scp -i ~/.ssh/earthquake-key.pem -r . ubuntu@54.169.123.45:~/earthquake-system
```

⚠️ **Lưu ý**: 
- Cách này sẽ copy cả `node_modules` nên sẽ lâu. Nên dùng Git.
- Trên Windows Git Bash, dùng `"D:/path"` thay vì `/d/path` để tránh lỗi.

### Cách 3: Sử dụng FileZilla (GUI)

1. Tải FileZilla: **https://filezilla-project.org/**
2. Mở FileZilla
3. Vào **Edit → Settings → SFTP → Add key file** → Chọn file `.pem`
4. Kết nối:
   - Host: `sftp://54.169.123.45`
   - Username: `ubuntu`
   - Port: `22`
5. Kéo thả thư mục project

---

## 8. Chạy Ứng Dụng

### 8.1 Cấu hình biến môi trường

SSH vào server và tạo file `.env`:

```bash
cd ~/earthquake-system

# Tạo file .env
cat > .env << 'EOF'
# Database
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_NAME=earthquake_db
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres123

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# API URLs (dùng tên container)
DATABASE_API_URL=http://database-api:8000
DATA_INGESTION_URL=http://data-ingestion:8001
DATA_ANALYSIS_URL=http://data-analysis:8002
CLUSTERING_URL=http://clustering:8003
PREDICTION_URL=http://prediction:8004

# USGS API
USGS_API_URL=https://earthquake.usgs.gov/fdsnws/event/1/query
EOF
```

### 8.2 Cập nhật Frontend để sử dụng IP thật

Sửa file `frontend/src/App.jsx` hoặc `frontend/.env`:

```bash
# Tạo file .env cho frontend
cat > frontend/.env << EOF
VITE_API_URL=http://54.169.123.45
EOF
```

Hoặc sửa file `App.jsx`:

```bash
# Sử dụng sed để thay đổi API URL
sed -i 's/localhost/54.169.123.45/g' frontend/src/App.jsx
```

### 8.3 Build và chạy Docker containers

```bash
cd ~/earthquake-system

# Build tất cả services
docker-compose build

# Chạy tất cả services (chế độ background)
docker-compose up -d
```

### 8.4 Kiểm tra trạng thái

```bash
# Xem tất cả containers
docker-compose ps
```

Output mong đợi:
```
NAME                    STATUS          PORTS
earthquake-postgres     Up              5432/tcp
earthquake-redis        Up              6379/tcp
earthquake-db-api       Up              0.0.0.0:8000->8000/tcp
earthquake-ingestion    Up              0.0.0.0:8001->8001/tcp
earthquake-analysis     Up              0.0.0.0:8002->8002/tcp
earthquake-clustering   Up              0.0.0.0:8003->8003/tcp
earthquake-prediction   Up              0.0.0.0:8004->8004/tcp
earthquake-frontend     Up              0.0.0.0:3000->3000/tcp
```

### 8.5 Xem logs

```bash
# Xem logs tất cả services
docker-compose logs

# Xem logs của service cụ thể
docker-compose logs -f frontend

# Xem 100 dòng logs cuối
docker-compose logs --tail=100
```

### 8.6 Truy cập ứng dụng

Mở trình duyệt và truy cập:

| Service | URL |
|---------|-----|
| **Frontend** | http://54.169.123.45:3000 |
| Database API | http://54.169.123.45:8000/docs |
| Data Analysis | http://54.169.123.45:8002/docs |
| Clustering | http://54.169.123.45:8003/docs |
| Prediction | http://54.169.123.45:8004/docs |

**Thay `54.169.123.45` bằng Public IP của bạn!**

---

## 9. Cấu Hình Domain (Tùy Chọn)

### 9.1 Đăng ký domain

Có thể mua domain từ:
- **Namecheap**: https://namecheap.com (~$10/năm cho .com)
- **GoDaddy**: https://godaddy.com
- **Google Domains**: https://domains.google
- **Route 53** (của AWS): https://aws.amazon.com/route53/

### 9.2 Cấu hình DNS

Thêm **A Record** trỏ về IP của EC2:

```
┌─────────────────────────────────────────────────────────────┐
│  DNS Records                                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Type    Name    Value              TTL                     │
│  ──────────────────────────────────────────────────────────│
│  A       @       54.169.123.45      3600                   │
│  A       www     54.169.123.45      3600                   │
│  A       api     54.169.123.45      3600                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 9.3 Cài đặt Nginx Reverse Proxy

```bash
# Cài đặt Nginx
sudo apt install -y nginx

# Tạo cấu hình
sudo nano /etc/nginx/sites-available/earthquake
```

Nội dung file:
```nginx
server {
    listen 80;
    server_name earthquake.yourdomain.com;

    # Frontend
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # API
    location /api/ {
        proxy_pass http://localhost:8000/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

Kích hoạt cấu hình:
```bash
sudo ln -s /etc/nginx/sites-available/earthquake /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

### 9.4 Cài SSL Certificate (HTTPS)

```bash
# Cài Certbot
sudo apt install -y certbot python3-certbot-nginx

# Lấy certificate
sudo certbot --nginx -d earthquake.yourdomain.com -d www.earthquake.yourdomain.com

# Tự động renew
sudo certbot renew --dry-run
```

---

## 10. Các Lỗi Thường Gặp

### Lỗi 1: "Connection refused" khi truy cập website

**Nguyên nhân:** Security Group chưa mở port

**Cách sửa:**
1. Vào EC2 Console → Security Groups
2. Chọn security group của instance
3. Edit Inbound rules → Add các port cần thiết

### Lỗi 2: Docker build failed - "no space left on device"

**Nguyên nhân:** Hết dung lượng ổ cứng

**Cách sửa:**
```bash
# Xóa Docker cache
docker system prune -a

# Kiểm tra dung lượng
df -h
```

### Lỗi 3: Container liên tục restart

**Nguyên nhân:** Service bị crash

**Cách kiểm tra:**
```bash
# Xem logs
docker-compose logs service-name

# Xem chi tiết container
docker inspect container-name
```

### Lỗi 4: Không kết nối được database

**Nguyên nhân:** Database container chưa ready

**Cách sửa:**
```bash
# Restart service
docker-compose restart database-api

# Hoặc restart tất cả
docker-compose down
docker-compose up -d
```

### Lỗi 5: "Module not found" khi build

**Nguyên nhân:** Thiếu dependencies

**Cách sửa:**
```bash
# Vào container và cài lại
docker-compose exec service-name pip install -r requirements.txt
```

### Lỗi 6: API timeout / chậm

**Nguyên nhân:** Instance quá nhỏ

**Cách sửa:**
1. Stop instance
2. Change instance type → t2.medium hoặc lớn hơn
3. Start instance

---

## 📝 Checklist Deploy

Sử dụng checklist này để đảm bảo không bỏ sót bước nào:

```
□ 1. Tạo tài khoản AWS
    □ Xác minh email
    □ Thêm thẻ thanh toán
    □ Chọn Basic Support (Free)

□ 2. Tạo Access Key
    □ Lưu Access Key ID
    □ Lưu Secret Access Key

□ 3. Cài đặt công cụ local
    □ Cài Git Bash
    □ Cài AWS CLI
    □ Chạy `aws configure`

□ 4. Tạo EC2 Instance
    □ Chọn Ubuntu 22.04
    □ Chọn t2.medium
    □ Tạo Key Pair (và lưu file .pem!)
    □ Cấu hình Security Group (mở ports)
    □ Storage 30GB

□ 5. Kết nối server
    □ SSH thành công
    □ Cài Docker
    □ Cài Docker Compose

□ 6. Deploy ứng dụng
    □ Clone/Upload code
    □ Tạo file .env
    □ Cập nhật API URL trong frontend
    □ docker-compose build
    □ docker-compose up -d

□ 7. Kiểm tra
    □ Truy cập Frontend
    □ Truy cập API docs
    □ Kiểm tra logs không có lỗi
```

---

## 💰 Quản Lý Chi Phí

### Cách kiểm tra chi phí

1. Vào **AWS Console** → **Billing Dashboard**
2. Xem **Cost Explorer** để theo dõi chi tiết

### Cách tiết kiệm

1. **Stop instance khi không dùng:**
```bash
# Từ local
aws ec2 stop-instances --instance-ids i-xxxxxxxxx
```

2. **Dùng Reserved Instances** nếu chạy lâu dài (tiết kiệm ~40%)

3. **Set Budget Alert:**
   - Vào Billing → Budgets → Create budget
   - Set alert khi chi phí vượt $10/tháng

### Dọn dẹp khi không dùng nữa

```bash
# Terminate instance (xóa vĩnh viễn)
aws ec2 terminate-instances --instance-ids i-xxxxxxxxx
```

Hoặc từ Console:
1. EC2 → Instances → Chọn instance
2. Instance state → Terminate instance

---

## 🎉 Hoàn Tất!

Chúc mừng! Bạn đã deploy thành công ứng dụng Earthquake Analysis System lên AWS!

**Các bước tiếp theo có thể làm:**
- [ ] Cấu hình domain riêng
- [ ] Thêm SSL certificate (HTTPS)
- [ ] Set up monitoring với CloudWatch
- [ ] Cấu hình auto-backup database
- [ ] Set up CI/CD với GitHub Actions

**Cần hỗ trợ?**
- AWS Documentation: https://docs.aws.amazon.com
- Docker Documentation: https://docs.docker.com
- Stack Overflow: https://stackoverflow.com

---

*Tài liệu được tạo cho Đồ án cuối kỳ - Môn Khai Phá Dữ Liệu*
